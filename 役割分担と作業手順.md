# Re:Place 役割分担と作業手順

対象: 4人チーム / ハッカソン当日
前提: 共通の土台（ナビゲーション・データ層・ジオフェンス通知）は構築済み。ここから各画面の実装に入る。

リポジトリ: https://github.com/hagikanta7-art/re-place

---

## 1. 画面遷移とファイルの対応

| 画面 | ファイル | 担当 |
|---|---|---|
| ①はじめに | `app/screens/OnboardingScreen.js` | D |
| ②場所一覧 | `app/screens/SpotListScreen.js` | A |
| ②地図タブ | `app/screens/SpotMapScreen.js` | A |
| ③場所登録・編集 | `app/screens/SpotFormScreen.js` | B |
| （③が使う地図ピン選択） | `app/MapPicker.js` | B |
| ④場所のカルテ | `app/screens/SpotDetailScreen.js` | C |
| ⑤記録追加・編集 | `app/screens/VisitFormScreen.js` | C |
| ⑥訪問履歴 | `app/screens/VisitHistoryScreen.js` | C |
| ⑦設定 | `app/screens/SettingsScreen.js` | D |
| （共通デザイン値） | `app/core/theme.js`（新規、Dが作成） | D |

ナビゲーションの骨格（`app/navigation/`）とデータ層・通知ロジック（`app/core/`）はすでに用意済み。各担当は自分の画面ファイルの中身を実装していく。

**ファイル所有権のルール**: 1ファイル＝1担当。他の人のファイルは**編集せず、importして使うだけ**にする。共通ファイル（`core/spots.js`, `core/theme.js`など）はそれぞれ1人の所有者だけが編集し、他の人はそこから関数や値を呼び出すだけにすることで、誰が触っても他人のファイルとコンフリクトしない状態を保つ。

---

## 2. 役割分担

### A: 画面基盤・場所一覧・地図タブ
- `screens/SpotListScreen.js`：検索・一覧表示の見た目を仕上げる（現状は最低限のリスト表示のみ）
- `screens/SpotMapScreen.js`：地図タブの見た目・ピンのポップアップ調整
- 必要であれば `navigation/` の調整（タブの見た目・アイコン等）
- **手が空いたら**：自分の担当画面のブラッシュアップ（アニメーション、空状態の見せ方など）を深掘りする

### B: 場所登録・編集 ＋ 写真アップロード
- `screens/SpotFormScreen.js`：現状は名前・位置・カテゴリ・通知ON/OFFまで実装済み
- `MapPicker.js`：③が使う地図ピン選択コンポーネントもB所有
- **やること**: 写真アップロード機能の追加（`expo-image-picker`等でカメラロール/撮影→リサイズ・圧縮→Base64化→保存時に含める）
- 場所の削除・編集の動作確認（`SpotDetailScreen`側に削除ボタンは実装済み、編集導線もつながっている）
- **手が空いたら**：写真の表示（一覧・カルテでのサムネイル表示）の作り込み

### C: カルテ詳細・記録の蓄積・通知（Must機能）
- `screens/SpotDetailScreen.js`：前回の記録表示、「＋今回の記録」「訪問履歴」への導線は実装済み。デザイン・細部を仕上げる
- `screens/VisitFormScreen.js`：記録の追加・編集フォーム（今回の内容／良かったこと／次回の注意点）。日付選択のUIは簡易版なので、時間があれば改善
- `screens/VisitHistoryScreen.js`：過去の記録一覧。デザイン調整
- `core/geofence.js`：ジオフェンス半径・通知タイミングの調整（現在は半径120m固定）
- 通知をタップしたときに④のカルテへ遷移する導線は実装済み。実機での動作確認を担当
- **手が空いたら**：通知本文のパターンを増やす（例: 記録が複数回ある場合の傾向表示）

### D: 設定画面 ＋ デザイン統一 ＋ 発表スライド
- `screens/SettingsScreen.js`：許可状況の表示・通知本文表示ON/OFFは実装済み。デザイン仕上げ
- `screens/OnboardingScreen.js`：説明文・見た目の作り込み
- `core/theme.js`（新規作成）：配色・フォントサイズ・余白などの共通値をここにまとめ、他の担当は自分の画面からこれを**import して使うだけ**にする。Dがここを更新すれば全画面のトーンが揃う仕組みにする（他人のファイルを直接編集しない）
- 発表スライドの作成
- **手が空いたら**：スライドの作り込みを優先。それでも余れば、自分の担当画面（設定・はじめに）をさらに作り込む

---

## 3. 各自の作業手順

### 3-1. 環境セットアップ（最初の1回だけ）

```powershell
git clone https://github.com/hagikanta7-art/re-place.git
cd re-place/app
npm install
```

### 3-2. 実機で動作確認する

すでにNode.jsのビルド（開発版・プレビュー版）がある場合、**JS側の変更だけならビルドし直す必要はありません**。

```powershell
npx expo start --dev-client
```

- ターミナルにQRコードが出るので、実機のアプリ（Re:Place）内から読み込んで接続する
- スマホとPCが同じWi-Fiに繋がっている必要あり（別ネットワークなら `--tunnel` オプションを追加）

**新しいライブラリを追加したとき**（例: Bさんが`expo-image-picker`を追加した場合）は、そのライブラリを使う機能を試す前に、誰か1人が新しいプレビュービルドを作り直す必要があります。担当者は作業を始める前にチームに一声かけてください。

接続後は、ファイルを保存するたびに実機の画面が自動更新される（Fast Refresh）。反映されない場合は実機を振って「Reload」、またはターミナルで `r` キー。

### 3-3. 作業の進め方（このループを繰り返す）

1. 作業前に `git pull` して最新化する
2. 自分の担当ファイル（上表参照）を編集・保存する。**他の人の担当ファイルを触る場合は先に一声かける**（コンフリクト防止）
3. `core/spots.js` のデータ操作関数（`createSpot`, `addVisit`, `updateVisit`など）は変更せず、そのまま呼び出して使う。もしデータ構造自体を変える必要が出てきたら、必ず全員に共有してから変更する
4. 実機で自動的に画面が更新されるので、その場で動作・UIを確認する（mergeを待つ必要はない）
5. 動いたら `git add` → `git commit` → `git push`（**1機能ごとに小さく、こまめに**。1日の終わりにまとめてやらない）

### 3-4. チーム全体での統合チェックポイント（1〜2時間おき推奨）

1. 作業中の人はキリの良いところで一旦 `push` する
2. 誰か1人が `git pull` して完全に最新化する
3. 実機でアプリをリロード（またはアプリを再起動）
4. ①〜⑦の画面を一通り触って、遷移や表示がおかしくないか確認する
5. 問題があればその場でチームに共有し、担当者がすぐ直す

こまめな push/pull とこのチェックポイントを回していれば、「最後に統合したら壊れていた」という事態を避けられる。

### 3-5. データ構造（共通の契約・勝手に変えない）

```
spots/{spotId}
- ownerId: string
- name: string
- category: string
- lat, lng: number
- notifyEnabled: boolean
- visits: [
    { date, content, goodPoint, caution, photoBase64 },
    ...
  ]
```

- `visits` は配列。新しい記録は末尾に追加（上書きしない）
- 画面をまたいで使うデータ操作は必ず `core/spots.js` の関数を経由する（各画面でFirestoreを直接叩かない）

---

## 4. 残っている主なタスク（優先度順）

1. **（Must）ジオフェンス通知の実機確認**：新しいプレビュービルドでも位置トリガー通知が機能するか、Cが最優先で確認
2. 写真アップロード（B）
3. 各画面のデザイン仕上げ（A・C・D）
4. 発表スライド（D）
5. 通知半径・タイミングの調整（C、時間があれば）

---

## 5. 困ったときは

- ビルドが必要か迷ったら：「新しいnpmパッケージを追加したか？」→ Yesならビルドし直しが必要
- データの形で迷ったら：本ファイルの「3-4」を参照、それでも不明なら全員に確認
- Firestoreのルールや権限まわりのエラーが出たら、コンソールで自分のデータかどうか（`ownerId`が自分のuidと一致しているか）を確認
